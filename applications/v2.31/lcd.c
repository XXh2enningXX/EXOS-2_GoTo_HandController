
#include "lcd.h"
#include <stdio.h>
#include <stdarg.h>

void InitLCD(void) {
    // Init your LCD here
}
void LCD_Clear(void) {
    // Clear screen
}
void LCD_Print(const char* fmt, ...) {
    // Temporärer Ersatz für LCD-Ausgabe:
    // z. B. auf UART oder Debug LED
    // Für jetzt: do nothing
}
